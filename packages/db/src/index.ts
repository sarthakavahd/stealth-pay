export { createSupabaseClient, getSupabaseClient, getSupabaseAdmin } from './client';
export type {
  Database,
  Profile,
  Wallet,
  WalletInsert,
  Transaction,
  TransactionInsert,
  DetectedPayment,
  DetectedPaymentInsert,
  ScannerState,
} from './database.types';
